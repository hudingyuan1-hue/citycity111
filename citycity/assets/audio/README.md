# Audio Placeholder

Place the first playable memory theme loop here:

`memory_theme_loop.ogg`

The current whitebox demo falls back to a generated in-engine test tone when this file is missing, so the game loop remains runnable before final audio is delivered.
